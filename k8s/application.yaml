apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: myapp-argo-application
  namespace: argocd
spec: 
  project: default

  source:
    repoURL: https://github.com/rima-gif/frontdevops
    targetRevision: HEAD
    path: K8s

  destination: 
    server: https://kubernetes.default.svc
    namespace: front

  syncPolicy:
    syncOptions:
    - CreateNamespace=true
    automated:
      prune: true
      selfHeal: true